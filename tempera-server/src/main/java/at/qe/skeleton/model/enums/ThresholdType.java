package at.qe.skeleton.model.enums;

/*
* It is not differentiated if the tolerance or limit has been passed, so only two types are used.
*
*/
public enum ThresholdType {
  UPPERBOUND_INFO,
  LOWERBOUND_INFO,
  UPPERBOUND_WARNING,
  LOWERBOUND_WARNING
}
